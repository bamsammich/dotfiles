# Config

```toml
[data]
email = "<your email>"

  [data.homebrew]
  brews = [ <list of brews to install> ]
  casks = [ <list of casks to install> ]

[data.kitty]
theme = "<theme name>"

[data.onepassword]
account = "<user id>"
```
