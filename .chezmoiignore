{{- $omarchy := not (not (stat (joinPath .chezmoi.homeDir ".local/share/omarchy"))) -}}
key.txt.age

{{/* ===== macOS-only: ignore on all Linux ===== */}}
{{ if ne .chezmoi.os "darwin" }}
.Brewfile
.zshrc
.zsh.d/**
.config/aerospace/**
.config/powershell/**
.config/zed/**
.pwsh/**
{{ end }}

{{/* ===== Linux-only: ignore on macOS ===== */}}
{{ if ne .chezmoi.os "linux" }}
.bashrc
.bash.d/**
{{ end }}

{{/* ===== Omarchy: allowlist — only sync universal paths ===== */}}
{{ if $omarchy }}
**
!.claude
!.claude/**
!.gitconfig
!.config
!.config/git
!.config/git/**
!.kube
!.kube/**
!decrypt-private-key.sh
!install-packages.sh
!bootstrap-yay.sh
{{ end }}

{{/* ===== Tests live beside what they test, but not in $HOME =====

     Last match wins, so this sits after the omarchy allowlist above rather
     than being re-included by its !.claude/** line. */}}
.claude/hooks/reviewd-gate.test.sh
